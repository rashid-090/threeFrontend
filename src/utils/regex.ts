const REGEX = {
  // phoneNumber: /[0-9]/,
  // aadhaar: /^[0-9]{12}$/,
  // pan: /[A-Z]{5}[0-9]{4}[A-Z]{1}/,
  // ifsc: /^[A-Z]{4}0[A-Z0-9]{6}$/,
  PINCODE: /^[0-9]{6}$/,
  CHARACTER_ONLY: /^[A-Za-z ]*$/,
};
export default REGEX;
